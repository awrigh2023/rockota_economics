// Removed: the "Travels of a T-Shirt" pilot deck (placeholder slides).
// Kept as an empty module because the file couldn't be deleted in this
// environment — safe to delete outright. Nothing imports it.
export {};
